apiVersion: v1
kind: ServiceAccount
metadata:
  name: opentracing-pod-annotator
  namespace: default
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: opentracing-pod-annotator
rules:
- apiGroups:
    - ""
  resources:
    - pods
  verbs:
    - get
    - list
    - watch
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: opentracing-pod-annotator
  namespace: default
subjects:
- kind: ServiceAccount
  name: opentracing-pod-annotator
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: opentracing-pod-annotator
---
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opentracing-pod-annotator
  namespace: default
  labels:
    app: opentracing-pod-annotator
spec:
  revisionHistoryLimit: 3
  replicas: 1
  selector:
    matchLabels:
      app: opentracing-pod-annotator
  template:
    metadata:
      labels:
        app: opentracing-pod-annotator
    spec:
      serviceAccountName: opentracing-pod-annotator
      containers:
        - name: opentracing-pod-annotator
          image: opentracing-pod-annotator:v0.1-3-ga448110
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: opentracing-pod-annotator
  namespace: default
  labels:
    app: opentracing-pod-annotator
spec:
  selector:
    app: opentracing-pod-annotator
  ports:
    - name: http
      port: 8080
  type: NodePort
